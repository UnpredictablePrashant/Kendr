# Tells node-gyp to build against Electron headers, not Node.js headers
# (electron-rebuild does this automatically, but this helps npm install)
runtime=electron
target=32.2.0
disturl=https://electronjs.org/headers
